  const symptoms=document.getElementById('galviscore-priorities'); const side=document.getElementById('galviscore-side-effects'); const care=document.getElementById('galviscore-care-steps');
  if(symptoms) symptoms.innerHTML=''; if(side) side.innerHTML=''; if(care) care.innerHTML='';
  ranked.forEach(([key])=>{ const c=scoreClinicalCopy(key); const s=document.createElement('li'); s.textContent=c.symptom; symptoms?.appendChild(s); const e=document.createElement('li'); e.textContent=c.side; side?.appendChild(e); const a=document.createElement('li'); a.textContent=c.care; care?.appendChild(a); });
  const cls=String(result.galviscore_classification||''); const top=ranked.slice(0,2).map(([k])=>labelize(k)).join(' and '); const ctx=document.getElementById('galviscore-watch-context');
  if(ctx) ctx.innerHTML=\`<strong>What should you watch?</strong><p>\${cls.includes('Critical')?'Your business is showing critical symptoms requiring immediate intervention.':cls.includes('Strained')?'Your business is showing material symptoms that require intervention before they compound.':cls.includes('Stable')?'Your business is stable, but symptoms are present and should be watched closely.':'Your business is showing stronger health, but unresolved symptoms still deserve timely care.'} Your highest priority symptoms are \${top||'the weakest business health signals'}.</p><p><strong>Continue to GalviShot™</strong> to understand what is creating these symptoms and what treatment should come first.</p>\`;
}
function renderUnlockedGalviScore(result){`);
  html = html.replace('renderPriorities(result.galviscore_top_priorities||\'\');', 'renderPriorities(result.galviscore_top_priorities||\'\'); renderGalviScoreClinicalContext(result);');
}
html = html.replace('Your Executive Clinical Findings Are Ready', 'Your GalviShot™ Is Ready');
html = html.replace('GalviScore™ showed where your venture may be strained. GalviShot™ explains what findings are showing up in your business, which symptoms matter most, and what to address before moving deeper into GalviSight™.', 'We identified the business conditions driving your symptoms. Unlock your GalviShot™ to see what needs treatment first and why.');
html = html.replace('<h1>Your Executive Clinical Findings</h1>', '<h1 class="center">Take Your GalviShots</h1>');
html = html.replace('<h3>Top Executive Findings</h3>', '<h3>GalviShot Prescriptions</h3>');
html = html.replace('<h3>Recommended Next Best Actions</h3>', '<h3>Strategic Care Steps</h3>');
html = html.replace('<h3>Strategic Risks</h3>', '<h3>Symptom Side Effects</h3>');
html = html.replace('GalviShot™ explains why your business is showing these symptoms. GalviSight™ turns those findings into clear actions so you know what to focus on next.', 'GalviShot™ identifies the treatment your business needs. GalviSight™ gives you the prescription — what to do next and why.');
html = html.replace(/const evidence=\(f\.evidence\|\|\[\]\)\.map\(e=>formatCustomerEvidence\(e\.display_value\)\)\.filter\(Boolean\)\.join\('; '\);\s*const fp=confidencePresentation\(f\.confidence \?\? result\.confidence\); node\.innerHTML=`[\s\S]*?`;/, `const evidence=(f.evidence||[]).map(e=>formatCustomerEvidence(e.display_value)).filter(Boolean).join('; '); const fp=confidencePresentation(f.confidence ?? result.confidence); node.innerHTML=\`<div class="gshot-finding-title">GalviShot Prescription \${i+1}</div><p><strong>Symptom:</strong> \${escapeHtml(f.title||f.finding_code||'Business symptom')}</p><p><strong>Prescription:</strong> \${escapeHtml(f.action||'Treat the highest priority business condition first.')}</p><p class="gshot-evidence"><strong>GalviLab Result:</strong> \${escapeHtml(evidence)}</p><p><strong>Why This Matters:</strong> \${escapeHtml(f.risk||f.finding_text||'Untreated business symptoms can become more expensive over time.')}</p><p class="small"><strong>Clinical Confidence:</strong> \${escapeHtml(fp.label)}</p>\`;`);
html = html.replace('<strong>Included in your $29 unlock:</strong>', '<strong>Unlock GalviSight™ to:</strong>');
html = html.replace('<li>Executive interpretation of the strongest supported findings</li><li>Prioritized hypotheses, risks, and opportunities</li><li>Recommended actions and urgency</li><li>Evidence trace and assumptions</li>', '<li>Understand what your symptoms are telling us</li><li>See what requires attention first</li><li>Know exactly what to do next and why</li><li>Understand how urgently you should act</li><li>See which GalviLab Results support your prescription</li>');
html = html.replace('Secure checkout powered by Stripe. GalviSight unlocks only after exact-product payment confirmation or the approved QA override.', '');
html = html.replace('Your GalviShot Findings Are Ready for Interpretation.', 'Your Business Prescription');
html = html.replace('<h3>GalviSight Hypotheses</h3>', '<h3>What Your Symptoms Are Telling Us</h3>');
html = html.replace('<h3>Strategic Risks</h3>', '<h3>Why This Requires Attention</h3>');
html = html.replace('<h3>Opportunities</h3>', '<h3>Prescription Opportunities</h3>');
html = html.replace('<h3>Recommended Actions</h3>', '<h3>Your GalviSight Prescription</h3>');
html = html.replace('GalviSight™ shows what actions deserve your attention. GalviPath™ organizes those actions into a focused 90 day path so you know how to move forward.', 'GalviSight™ gives you the prescription. GalviPath™ gives you the treatment plan — when to act, what order to follow, what to monitor, and when additional care may be needed.');
html = html.replace(/const urgency=result\.urgency;[\s\S]*?setSectionVisibility\('galvisight-urgency-wrap',Boolean\(String\(urgencyText\|\|''\)\.trim\(\)\)\);/, `const urgency=result.urgency; const urgencyText=typeof urgency==='object'?(urgency.label||urgency.level||urgency.statement||''):String(urgency||''); const horizon=typeof urgency==='object'?(urgency.horizon||''):''; const reason=typeof urgency==='object'?(urgency.reason||''):''; if(el('galvisight-urgency')) el('galvisight-urgency').textContent=[urgencyText,horizon].filter(Boolean).join(' — '); const wrap=el('galvisight-urgency-wrap'); if(wrap&&reason){ let p=wrap.querySelector('.galvisight-urgency-reason'); if(!p){p=document.createElement('p');p.className='galvisight-urgency-reason';wrap.appendChild(p);} p.textContent='Why this is urgent: '+reason; } setSectionVisibility('galvisight-urgency-wrap',Boolean(String(urgencyText||'').trim()));`);
html = html.replace('GalviPath™ turns your GalviCare™ findings into a focused 90 day plan. It shows what to work on first, which actions to take, what progress to watch for, and when GalviClinic™ support may help you move forward.', 'GalviSight™ gives you the prescription. GalviPath™ gives you the 90 Day Business Treatment Plan — when to act, what order to follow, what to monitor, and when additional care may be needed.');
html = html.replace('Secure checkout powered by Stripe. GalviPath unlocks only after exact-product payment confirmation or the approved QA override.', '');
html = html.replace("'Evidence to Collect'", "'GalviLab Samples to Collect'");
html = html.replace("heading.textContent='Operating Cadence';", "heading.textContent='Initial GalviPath Check Up Discussion Topic';");
html = html.replace("'Escalation Triggers'", "'Future GalviPath Discussions'");
if (!html.includes('function openGalviPathCheckUp')) {
  html = html.replace("function openGalviClinicHandoff(){", `function openGalviPathCheckUp(){ if(typeof fireGalviEvent==='function'){try{fireGalviEvent('galvipath_checkup_clicked',{product:'galvipath',session_id:sessionId(),stage:'GalviPath Check Up'});}catch(e){}} window.open(GALVIPATH_CHECKUP_URL,'_blank','noopener'); }
    function openGalviClinicHandoff(){`);
}
if (!html.includes("checkupButton.textContent='GalviPath Check Up'")) {
  html = html.replace("cadence.appendChild(copy);\n        panel.appendChild(cadence);", "cadence.appendChild(copy); const checkupButton=document.createElement('button'); checkupButton.type='button'; checkupButton.className='secondary-button'; checkupButton.textContent='GalviPath Check Up'; checkupButton.addEventListener('click',openGalviPathCheckUp); cadence.appendChild(checkupButton); panel.appendChild(cadence);");
  html = html.replace("buttonRow.appendChild(printButton);\n      buttonRow.appendChild(clinicButton);", "buttonRow.appendChild(printButton); const pathCheckupButton=document.createElement('button'); pathCheckupButton.type='button'; pathCheckupButton.className='secondary-button'; pathCheckupButton.textContent='GalviPath Check Up'; pathCheckupButton.addEventListener('click',openGalviPathCheckUp); buttonRow.appendChild(pathCheckupButton); buttonRow.appendChild(clinicButton);");
}
html = html.replace('Your GalviPath™ gives you a clear direction for the next 90 days. GalviClinic™ gives you dedicated support to work through your priorities, make decisions, and keep your business moving forward.', 'Your GalviPath™ gives you the treatment plan. GalviClinic™ provides focused live Business Physician care to address the root cause of your most serious business symptoms and help your venture recover, stabilize, and grow.');
if (!html.includes("'GalviCare Markers'")) {
  html = html.replace("renderGalviPathListSection(\n        panel,\n        'Future GalviPath Discussions',", "renderGalviPathListSection(panel,'GalviCare Markers',result.galvicare_markers,false);\n      renderGalviPathListSection(panel,'GalviCare Recovery Indicators',result.recovery_indicators,false);\n\n      renderGalviPathListSection(\n        panel,\n        'Future GalviPath Discussions',");
}
if (!worker.includes('function clinicalDimensionLanguage')) {
  const insertAt = 'function findingLibrary() { return [';
  if (!worker.includes(insertAt)) throw new Error('SOURCE DRIFT: findingLibrary');
  const clinicalHelpers = `function clinicalDimensionLanguage(key){ return ({
  customer:{condition:'customer acquisition and value understanding are unstable',treatment:'narrow the customer focus and validate the highest value outcome with direct conversations',prescription:'For the next 14 days, focus outreach on the customer segment showing the strongest buying signal, define what qualifies a real opportunity, and stop treating low-intent attention as demand.',marker:'qualified opportunities and customer conversations',recovery:'a larger share of conversations convert into qualified opportunities'},
  revenue:{condition:'revenue or funding predictability is under pressure',treatment:'stabilize the repeatable commercial signal before adding complexity',prescription:'Within 7 days, identify the revenue, conversion, retention, or funding signal creating the greatest uncertainty. Track it weekly and direct effort toward the activity most directly connected to improving it.',marker:'revenue predictability, conversion, retention, or funding progress',recovery:'the selected commercial signal improves for two consecutive review periods'},
  product:{condition:'product learning is not consistently strengthening customer value',treatment:'turn the strongest customer feedback signal into one proof of value improvement',prescription:'Choose the customer outcome that matters most, make one targeted offer or product improvement tied to that outcome, and validate whether customers experience the intended improvement before expanding scope.',marker:'customer feedback and proof of value',recovery:'customer feedback consistently confirms the targeted outcome is improving'},
  distribution:{condition:'qualified demand is not arriving through a repeatable channel',treatment:'concentrate acquisition on the channel most likely to create qualified demand',prescription:'For the next 14 days, concentrate acquisition on the channel producing the clearest qualified demand signal, define a qualified lead threshold, and stop spending time on channels that create attention without meaningful progression.',marker:'qualified demand by channel',recovery:'one channel consistently creates qualified demand above the defined threshold'},
  problem:{condition:'the urgency of the customer problem is not yet proven strongly enough',treatment:'validate the problem before investing further in the solution',prescription:'Interview the customers closest to the problem, document how often it occurs and what it costs them, and do not expand solution scope until the urgency signal becomes clear.',marker:'problem frequency, urgency, and customer willingness to act',recovery:'customers consistently describe the problem as urgent and take a measurable next step'},
  business_model:{condition:'the business model still depends on unresolved assumptions or founder dependency',treatment:'identify and test the model assumption that most affects repeatability',prescription:'Name the assumption that would most damage the model if it proved false, define a measurable test, and resolve that assumption before increasing fixed cost or operating complexity.',marker:'repeatability, founder dependency, and model assumption validation',recovery:'the model produces the intended outcome without requiring the founder to manually rescue the process'},
  leadership:{condition:'decision load and follow through are constraining execution',treatment:'reduce competing priorities and establish a clear decision sequence',prescription:'Within 7 days, name the three decisions only leadership can make, rank them by business consequence, assign an owner and deadline, and review completion before adding another strategic priority.',marker:'decision cycle time and completion of owner-level priorities',recovery:'critical decisions are made on time and reliably convert into completed action'},
